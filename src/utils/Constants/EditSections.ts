export const SECTION_KEYS = [
  'personalDetails',
  'contactInfo',
  'emergencyContacts',
  'addMedication',
  'reports',
  'healthParameters',
];

export type SectionKey = (typeof SECTION_KEYS)[number];
