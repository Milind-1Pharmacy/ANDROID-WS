import React from 'react';
import {Text} from 'react-native';
import {chipProps} from '../types';

const AddMediationForm: React.FC<chipProps> = ({initialData, handleSubmit}) => {
  return <Text>AddMediationForm</Text>;
};

export default AddMediationForm;
